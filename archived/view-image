{
    "name": "view-image",
    "description": "Shows an image from an URL. Author: grosik",
    "executeit": "(data) => { let url = data.split(' ')[1]; if(url == undefined || !(urlregex.test(url))) return 'Provide image URL!'; log(`<img src=\"${url}\">`); return '-' }"
}
