{
    "name": "time",
    "description": "Tells the time",
    "author": "grosik",
    "executeit": "(data) => { let date = new Date(); return `<h2>${date.getHours().toString().padStart(2, '0')}:${date.getMinutes().toString().padStart(2, '0')}:${date.getSeconds().toString().padStart(2, '0')}</h2>`; }"
}
