{
    "name": "change-prefix",
    "description": "Changes the command prefix. <span class=\"yellow\">Outdated!</span>",
    "author": "grosik",
    "executeit": "(data) => { prefix = data.replace('change-prefix ', '').replaceAll('|&lt;', '<').replaceAll('|&gt;', '>'); return `<span class=\"green\">Changed the command prefix to <span class=\"blue\">${data.replace('change-prefix ', '').replaceAll('-&lt;', '<').replaceAll('-&gt;', '>')}</span></span>.`; }"
}
