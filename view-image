{
    "name": "view-image",
    "description": "Shows an image from an URL. Author: grosik",
    "executeit": "(data) => { let url = data.split(' ')[1]; if(url == undefined) return 'Provide image URL!'; log(`<img src=\"${url}\">`); return '-' }"
}
